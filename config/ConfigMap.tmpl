---
kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ .AppName }}
  labels:
    configVersion: v1
data:
{{ range $key, $value := .Vars }}  {{ $key }}: {{ if typeIsLike "string" $value }}{{ $value }}{{ else }}"{{ $value }}"{{ end }}
{{ end }}